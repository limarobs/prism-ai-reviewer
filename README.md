# PRism AI Reviewer

Evidence-based AI code review for GitHub pull requests, powered by Gemini.

## Development

Requirements:

- Node.js 22+
- npm 10+

Install dependencies and start both applications:

```bash
npm install
npm run dev
```

The web application runs on `http://localhost:5173` and the API on
`http://localhost:8787`.
